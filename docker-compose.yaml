version: "3.8"
services:

  backend:
    image: backend:numbersapi
    environment: 
      - SERVER_LISTEN_ADDRESS=8080

  frontend:
    image: frontend:numbersapi
    ports:
      - "8081:80"
    environment:
      - REACT_APP_BACKEND_URL=http://backend:8080
    links:
      - backend
    